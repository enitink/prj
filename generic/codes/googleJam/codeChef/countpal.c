#include <stdio.h>
#include <string.h>

int main(){
	char ch;
	int arr[1001], i = 0;
	scanf("%c",&ch);
	memset(arr, 0, sizeof(arr));
	while(('\n' != ch) && ('\0' != ch)){
		
		scanf("%c",&ch);
	}
	return 0;
}
